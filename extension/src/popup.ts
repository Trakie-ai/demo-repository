// Trakie Extension — Popup entry point
// Will handle QR code display and Socket.IO connection status in W2

document.addEventListener('DOMContentLoaded', () => {
  console.log('Trakie popup loaded');
});
